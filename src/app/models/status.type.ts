export interface Status {
  id: number;
  status_name: string;
  project_id: number;
  status_color: string;
}

export interface StatusFields {
  status_name: string;
  project_id: number;
  status_color: string;
}
